# ── Dependencias para RODAR A SUITE (tests/, menos tests/api) ────────────────
# Instalar com:  pip install -r requirements-test.txt
#
# POR QUE ESTE ARQUIVO EXISTE
# O CI instalava o requirements.txt inteiro para rodar os testes. Isso arrastava
# dbt, prefect, streamlit e pysus -- nada disso e importado pela suite -- e a
# fragilidade cobrou: `dbt-core` puxou `dbt-core-experimental-parser 2.0.0b1`,
# um beta publicado sem wheel cuja metadata nao compila, e o job quebrou em
# Python 3.11 por um motivo sem nenhuma relacao com o codigo testado.
#
# A lista abaixo saiu de varredura por AST dos imports de tests/, validation/,
# ingestion/, ml/ e dos scripts que os testes importam. Ficaram de fora os
# imports LAZY (psycopg2 em validation/loader.py, datasus_dbc e dbfread em
# pipeline_sih_hospitalar.py), que so acontecem dentro de funcoes que a suite
# nao chama, e o prophet, cujos testes ja se marcam como skip quando falta.
#
# O QUE SE PERDE: o CI deixa de provar que requirements.txt instala. Esse teste
# tinha valor -- foi ele que revelou o conflito loguru x pysus -- mas ele estava
# preso ao job errado, derrubando os testes por causa da arvore de deploy.
#
# Versoes acompanham requirements.txt de proposito. O teto de loguru vem de la
# (pysus fixa <0.7.0): se a suite rodasse em 0.7 e a producao em 0.6, um teste
# poderia passar usando API que nao existe onde o codigo roda.

pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0
psycopg[binary]>=3.1.0
asyncpg>=0.29.0
great-expectations>=0.18.0
loguru>=0.6.0,<0.7.0
requests>=2.34.2
tenacity>=8.2.0
click>=8.1.0
httpx>=0.26.0

# Ferramentas
#
# Os pinos estavam em pytest 8.3.4 / pytest-asyncio 0.24 / pytest-cov 6.0 /
# ruff 0.8.4 enquanto o ambiente local ja rodava 9.1.1 / 1.4.0 / 7.1.0 / 0.16.2.
# A divergencia nao era teorica: o job de supply chain, na sua primeira
# execucao, acusou PYSEC-2026-1845 no pytest 8.3.4 (diretorio previsivel em
# /tmp/pytest-of-{user} no UNIX, com DoS local e possivel escalada) -- algo que
# NENHUMA execucao local encontraria, porque o pino vulneravel so existia aqui.
#
# Risco real baixo: e o runner de teste, nao codigo publicado. Mas o custo de
# alinhar e zero e o ganho e duplo -- fecha a vulnerabilidade e faz o CI rodar a
# mesma coisa que a maquina de quem desenvolve, que era a lacuna de
# reprodutibilidade apontada em docs/SECURITY_AUDIT.md.
#
# As versoes abaixo sao as que a suite de 499 testes ja passa localmente em
# 3.11. O CI tambem roda 3.12; se divergir la, o job dira.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.16.2

# Import do servidor MCP conferido pelo CI.
#
# O comentario aqui dizia "teto <2: a 2.0.0 removeu mcp.server.fastmcp" -- ou
# seja, justificava um teto ABAIXO de 2.0.0, enquanto a linha seguinte EXIGE
# 2.x. Ficou para tras quando o servidor migrou de FastMCP para MCPServer (a
# 2.0.0 renomeou a classe, e o codigo em mcp_server/ ja usa o nome novo). Quem
# lesse o comentario e "corrigisse" o pino para <2 quebraria o pacote publicado
# no PyPI. O teto <3 continua valendo: e a proxima major, ainda nao avaliada.
mcp>=2,<3

# ── Pisos de segurança (transitivas) ────────────────────────────────────────
# Estas três não são importadas diretamente por nada aqui: `cryptography` vem de
# great-expectations e PyJWT, `starlette` vem do SDK `mcp`. Entram assim mesmo
# porque `pip-audit` acusou cinco vulnerabilidades conhecidas nelas, e sem piso
# explícito uma instalação nova pode resolver para a versão vulnerável de novo.
#
# Nenhuma é alcançável nesta arquitetura — não há servidor HTTP em produção, e o
# servidor MCP fala por stdio, sem parsing de formulário — mas o custo de fixar
# o piso é zero e a análise completa está em docs/SECURITY_AUDIT.md.
cryptography>=50.0.0     # PYSEC-2026-3552/3553/3554
starlette>=1.3.1         # PYSEC-2026-249 (limites ignorados em urlencoded)
pydantic-settings>=2.14.2  # GHSA-4xgf-cpjx-pc3j (symlink em secrets_dir)

# --- Analise nao supervisionada (scripts/analise_perfil_mortalidade.py) ---
# scipy ja era usado por pipeline_v2 (IC95% gamma) sem estar declarado aqui;
# scikit-learn entra com a analise de perfil de mortalidade (k-means, ARI,
# silhueta). Ambos passam a ser explicitos: dependencia usada e nao declarada
# quebra o CI no dia em que um teste toca o modulo que a importa.
scipy>=1.11.0
scikit-learn>=1.4.0
