# ============================================================
# saude-publica-br — requirements.txt
# Instale com: pip install -r requirements.txt
# ============================================================

# --- Data / Ingestão ---
pysus>=0.11.0
pyarrow>=14.0.0
pandas>=2.1.0
numpy>=1.26.0
duckdb>=0.9.0

# --- Banco de dados ---
psycopg[binary]>=3.1.0
asyncpg>=0.29.0
supabase>=2.0.0
sqlalchemy>=2.0.0

# --- dbt ---
dbt-core>=1.7.0
dbt-postgres>=1.7.0
dbt-duckdb>=1.7.0

# --- API ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.14.2  # GHSA-4xgf-cpjx-pc3j
redis[asyncio]>=5.0.0
httpx>=0.26.0

# --- Dashboard ---
streamlit>=1.31.0
plotly>=5.18.0

# --- Orquestração / QA ---
prefect>=2.14.0
great-expectations>=0.18.0

# --- Utilitários ---
python-dotenv>=1.0.0
# O teto vem do pysus, que fixa loguru<0.7.0 em TODAS as versoes, ate a 2.7.0.
# Com `loguru>=0.7.0` este arquivo era insatisfazivel e `pip install -r
# requirements.txt` nunca completava — por isso o ambiente local sempre foi
# montado pacote a pacote. A API usada (add, remove, info, success, warning)
# e a mesma nas duas linhas. Para voltar ao 0.7, o caminho e sair do pysus.
loguru>=0.6.0,<0.7.0
tenacity>=8.2.0
click>=8.1.0
rich>=13.7.0
tqdm>=4.66.0

# ── Pisos de segurança (transitivas) ────────────────────────────────────────
# Estas três não são importadas diretamente por nada aqui: `cryptography` vem de
# great-expectations e PyJWT, `starlette` vem do SDK `mcp`. Entram assim mesmo
# porque `pip-audit` acusou cinco vulnerabilidades conhecidas nelas, e sem piso
# explícito uma instalação nova pode resolver para a versão vulnerável de novo.
#
# Nenhuma é alcançável nesta arquitetura — não há servidor HTTP em produção, e o
# servidor MCP fala por stdio, sem parsing de formulário — mas o custo de fixar
# o piso é zero e a análise completa está em docs/SECURITY_AUDIT.md.
cryptography>=50.0.0     # PYSEC-2026-3552/3553/3554
starlette>=1.3.1         # PYSEC-2026-249 (limites ignorados em urlencoded)
