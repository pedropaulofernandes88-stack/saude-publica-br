# ── Dev / CI dependencies ─────────────────────────────────────────────────────
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Linting & formatting
ruff==0.8.4

# Type checking
mypy==1.13.0
types-redis==4.6.0.20241004

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.28.1          # async test client for FastAPI (TestClient)
